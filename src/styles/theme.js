import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#f0e9e5',
    },
    secondary: {
      main: '#573e32',
    },
  },

  typography: {
    p: {
      fontSize: '1rem',
    },
  },
});
